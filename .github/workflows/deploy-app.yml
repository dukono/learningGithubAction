name: CI/CD Maven - App
on:
  push:
    branches: [main]
    paths:
      - app/**
      - .github/workflows/deploy-app.yml
env:
  NAME: "App"
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: Preparar nombre de repositorio en minúsculas
        run: |
          echo "REPO_LC=${GITHUB_REPOSITORY@L}" >> ${GITHUB_ENV}
          echo "DEPLOY_TAG=ghcr.io/${{env.NAME}}/${{env.REPO_LC}}:${{ github.sha }}" >> ${GITHUB_ENV}
          echo DEPLOY_TAG:${{env.GITHUB_ENV}}
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build y test
        run: |
          cd app
          mvn -B package

      - name: Login a GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build y Push imagen Docker
        uses: docker/build-push-action@v7
        with:
          file: app/dockerfile
          context: app
          push: true
          tags: ${{ env.DEPLOY_TAG }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Preparar nombre de repositorio en minúsculas
        run: echo "REPO_LC=${GITHUB_REPOSITORY@L}" >> ${GITHUB_ENV}

      - name: Deploy en Northflank
        uses: northflank/deploy-to-northflank@v1
        with:
          northflank-api-key: ${{ secrets.NORTHFLANK_API_KEY }}
          project-id: ${{ secrets.NORTHFLANK_PROJECT_ID }}
          service-id: ${{ secrets.NORTHFLANK_SERVICE_ID }}
          image-path: ${{ env.DEPLOY_TAG }}
          credentials-id: ${{ secrets.NORTHFLANK_CREDENTIALS_ID }}
